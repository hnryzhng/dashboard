// storeData.js
// store in processed data object in dataSource collection



var storeData = function(record) {

	// check to see if dataSource_id exists since content hash
	// if not, then store dataSource_id, name, description, and data in collection dataSources




};

module.exports = storeData;

